/** Signed-in home is Mail. Overview is furniture at /overview. */
export const SIGNED_IN_HOME = "/messages";

/** Why compose cannot send this week: no sending identity, SES sandbox. */
export const SEND_HALT_REASON = "Sending is paused — this workspace has no sending identity yet.";
