{
  "name": "@rootmail/cli",
  "version": "0.2.0",
  "description": "Command-line interface for rootmail — send, inspect, import, and ask the assistant from your terminal or CI.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "rootmail": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rootmail/node": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  }
}
